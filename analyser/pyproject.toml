[project]
name = "analyser"
version = "0.1.0"
description = "Monies OI Flow Analyser for NIFTY, BANKNIFTY, SENSEX"
requires-python = ">=3.12"
dependencies = [
    "kiteconnect>=5.0.0",
    "pandas>=2.0.0",
    "pytz>=2024.1",
    "python-dotenv>=1.0.0",
]
